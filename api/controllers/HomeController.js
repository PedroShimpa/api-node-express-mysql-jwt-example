const path = require('path');

module.exports = () => {
    const controller = {};

    // controller.home = async (req, res) => {
    //     res.sendFile(path.join(/index.html'));
    // }

    return controller;
}